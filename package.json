{
  "name": "image-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for image processing",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "lint": "eslint ."
  },
  "keywords": ["aws", "lambda", "image", "processing"],
  "author": "Your Name <your.email@example.com>",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "serverless": "^2.66.2"
  }
}
